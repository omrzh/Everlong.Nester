<Project>
  <PropertyGroup>
    <!-- The product version, declared once.  Bump AppVersion; the three below follow. -->
    <AppVersion>0.1.8</AppVersion>
    <Version>$(AppVersion)</Version>
    <FileVersion>$(AppVersion)</FileVersion>
    <AssemblyVersion>$(AppVersion)</AssemblyVersion>
  </PropertyGroup>
</Project>
