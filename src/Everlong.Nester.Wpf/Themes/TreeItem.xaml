<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:v="clr-namespace:Everlong.Nester.Controls">

  <ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="Base.xaml" />
  </ResourceDictionary.MergedDictionaries>

  <BooleanToVisibilityConverter x:Key="BoolToVisTree" />

  <Style TargetType="{x:Type v:TreeItem}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type v:TreeItem}">
          <StackPanel>

            <!--
              Header row: Grid splits content area from expand button.
            -->
            <Grid MinHeight="36">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>

              <Button x:Name="PART_HeaderButton"
                      Grid.Column="0"
                      Background="Transparent"
                      Foreground="{TemplateBinding Foreground}"
                      FontWeight="{TemplateBinding FontWeight}"
                      BorderThickness="0"
                      HorizontalAlignment="Stretch"
                      HorizontalContentAlignment="Stretch"
                      VerticalContentAlignment="Center"
                      Cursor="Hand"
                      Margin="0,1"
                      Padding="0">
                <Button.Template>
                  <ControlTemplate TargetType="Button">
                    <Border x:Name="HeaderBorder"
                            Background="{TemplateBinding Background}"
                            CornerRadius="4">
                      <ContentPresenter x:Name="HeaderContent"
                                        Content="{Binding Header, RelativeSource={RelativeSource AncestorType={x:Type v:TreeItem}}}"
                                        ContentTemplate="{Binding HeaderTemplate, RelativeSource={RelativeSource AncestorType={x:Type v:TreeItem}}}"
                                        VerticalAlignment="Center"
                                        Margin="8,0,0,0" />
                    </Border>
                    <ControlTemplate.Triggers>
                      <!-- Hover -->
                      <Trigger Property="IsMouseOver" Value="True">
                        <Setter TargetName="HeaderBorder" Property="Background"
                                Value="{DynamicResource Nester.TreeItem.Hover}" />
                      </Trigger>
                    </ControlTemplate.Triggers>
                  </ControlTemplate>
                </Button.Template>
              </Button>

              <!-- Expand/collapse button with chevron; shown only when item has children -->
              <Button x:Name="PART_ExpandButton"
                      Grid.Column="1"
                      Width="32"
                      Background="Transparent"
                      BorderThickness="0"
                      Padding="0"
                      Cursor="Hand"
                      Visibility="Collapsed">
                <Viewbox Width="16" Height="16" Stretch="Uniform">
                  <Canvas Width="24" Height="24">
                    <Path x:Name="ChevronIcon"
                          Fill="#6B7280"
                          Data="{DynamicResource Nester.Icon.TreeItem.Chevron.Collapsed}" />
                  </Canvas>
                </Viewbox>
              </Button>

            </Grid>

            <!-- Children host: indented, collapsed until IsExpanded=true -->
            <Border x:Name="ChildrenHost"
                    Margin="{DynamicResource Nester.TreeItem.IndentMargin}"
                    Visibility="Collapsed">
              <ItemsPresenter />
            </Border>

          </StackPanel>
          <ControlTemplate.Triggers>

            <!-- Show children when expanded -->
            <Trigger Property="IsExpanded" Value="True">
              <Setter TargetName="ChildrenHost" Property="Visibility" Value="Visible" />
            </Trigger>

            <!-- Show expand button when item has children -->
            <DataTrigger Binding="{Binding HasItems, RelativeSource={RelativeSource TemplatedParent}}"
                         Value="True">
              <Setter TargetName="PART_ExpandButton" Property="Visibility" Value="Visible" />
            </DataTrigger>

            <!-- Chevron: expanded state -->
            <Trigger Property="IsExpanded" Value="True">
              <Setter TargetName="ChevronIcon" Property="Data"
                      Value="{DynamicResource Nester.Icon.TreeItem.Chevron.Expanded}" />
            </Trigger>

            <!-- Disabled -->
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" Value="0.5" />
            </Trigger>

          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>
