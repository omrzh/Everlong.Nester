namespace Everlong.Nester.Shell;

/// <summary>
///   The WPF window-status snapshot — the shell mirrors the host window's
///   state into it via <see cref="IWpfShell.FeedHostPropertyChanged" />.
/// </summary>
public sealed class HostStatus : HostPropertyBase
{
}
