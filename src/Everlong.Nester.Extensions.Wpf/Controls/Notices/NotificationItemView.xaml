<local:FeedbackItemViewBase x:Class="Everlong.Nester.Controls.NotificationItemView"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Everlong.Nester.Controls"
                    xmlns:n="clr-namespace:Everlong.Nester.Notice;assembly=Everlong.Nester.Extensions"
                    xmlns:conv="clr-namespace:Everlong.Nester.Converters"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    mc:Ignorable="d" d:DataContext="{d:DesignInstance n:NotificationEntry}"
                    Background="Transparent" Width="350"
                    HorizontalAlignment="Right"
                    Margin="0,0,0,10">
  <local:FeedbackItemViewBase.Resources>
    <conv:TypeToIconConverter x:Key="TypeToIconConverter" />
    <conv:TypeToBrushConverter x:Key="TypeToBrushConverter" />
    <Style x:Key="BannerCloseButtonStyle" TargetType="Button">
      <Setter Property="Width" Value="24" />
      <Setter Property="Height" Value="24" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="Margin" Value="4,0,0,0" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="VerticalAlignment" Value="Top" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bg"
                    Background="{TemplateBinding Background}"
                    CornerRadius="12">
              <ContentPresenter HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bg" Property="Background"
                        Value="{DynamicResource Nester.Notice.Hover}" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Bg" Property="Background"
                        Value="{DynamicResource Nester.Border}" />
                <Setter Property="Opacity" Value="0.85" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </local:FeedbackItemViewBase.Resources>

  <!-- The outer Border carries the rounded shadow only (transparent background, same corner
       radius), so the shadow follows the corner; the inner Border is the card itself. -->
  <Border CornerRadius="{DynamicResource Nester.CornerRadius.Medium}"
          Background="Transparent">
    <Border.Effect>
      <DropShadowEffect Color="#40000000" BlurRadius="{DynamicResource Nester.BlurRadius}" ShadowDepth="4"
                        Direction="270" Opacity="0.4" />
    </Border.Effect>
    <Border CornerRadius="{DynamicResource Nester.CornerRadius.Medium}"
            Background="{DynamicResource Nester.Toast.Background}"
            BorderBrush="{DynamicResource Nester.Border}"
            BorderThickness="1"
            Padding="{DynamicResource Nester.Padding.Medium}">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Icon -->
        <Path Grid.Column="0"
              Data="{Binding Level, Converter={StaticResource TypeToIconConverter}}"
              Fill="{Binding Level, Converter={StaticResource TypeToBrushConverter}}"
              Margin="0,0,12,0"
              VerticalAlignment="Top"
              Stretch="Uniform"
              Width="{DynamicResource Nester.Icon.Size.Small}"
              Height="{DynamicResource Nester.Icon.Size.Small}" />

        <!-- Content -->
        <StackPanel Grid.Column="1" VerticalAlignment="Center">
          <TextBlock Text="{Binding Title}" FontWeight="Bold" Margin="0,0,0,4"
                     Foreground="{DynamicResource Nester.Toast.Foreground}">
            <TextBlock.Style>
              <Style TargetType="TextBlock">
                <Style.Triggers>
                  <DataTrigger Binding="{Binding Title}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed" />
                  </DataTrigger>
                  <DataTrigger Binding="{Binding Title}" Value="">
                    <Setter Property="Visibility" Value="Collapsed" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </TextBlock.Style>
          </TextBlock>
          <TextBlock Text="{Binding Message,FallbackValue=Text}" TextWrapping="Wrap"
                     Foreground="{DynamicResource Nester.Toast.Foreground}" />
          <Button Content="{Binding ActionText}"
                  Command="{Binding ActionCommand}"
                  Margin="0,8,0,0"
                  HorizontalAlignment="Left"
                  Visibility="{Binding ActionText, Converter={StaticResource StringToVisibilityConverter}}"
                  Background="Transparent"
                  BorderThickness="0"
                  Padding="0" />
        </StackPanel>

        <!-- Close Button -->
        <Button Grid.Column="2"
                Style="{StaticResource BannerCloseButtonStyle}"
                Command="{Binding DismissCommand}">
          <Path Data="M 2,2 L 14,14 M 14,2 L 2,14"
                Stroke="{DynamicResource Nester.Toast.Foreground}"
                StrokeThickness="1.4"
                StrokeStartLineCap="Round"
                StrokeEndLineCap="Round"
                Width="16" Height="16" Stretch="None" />
        </Button>
      </Grid>
    </Border>
  </Border>
</local:FeedbackItemViewBase>
