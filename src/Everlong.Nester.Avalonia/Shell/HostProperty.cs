namespace Everlong.Nester.Shell;

/// <summary>
///   The Avalonia window-status snapshot — the shell mirrors the host
///   window's state into it via
///   <see cref="IAvaloniaShell.FeedHostPropertyChanged" />.
/// </summary>
public sealed class HostProperty : HostPropertyBase
{
}
