<Project>

  <PropertyGroup>
    <!-- The build host is not shipped, so it does not take the root's versions. -->
    <ManagePackageVersionsCentrally>false</ManagePackageVersionsCentrally>
  </PropertyGroup>

</Project>
